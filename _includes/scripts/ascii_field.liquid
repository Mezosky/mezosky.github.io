<!--
  Parallax for the ASCII backdrop; the artwork itself needs no JavaScript.
  Loaded on every page rather than only where a field exists, so it can rebind
  after a soft navigation.
-->
<script defer src="{{ '/assets/js/ascii-field.js' | relative_url | bust_file_cache }}"></script>
